
version: 2

models:
  - name: 310_430_DIM_UNITE_URBAINE
    config:
      materialized: incremental
      database: "{{ target.database }}"
      schema: "DWH"
      alias: DIM_UNITE_URBAINE
      unique_key: uu
      incremental_strategy: merge
      merge_exclude_columns: ['inserted_at']
    columns:
    - name: uu
      data_type: varchar(5)
      tests:
        - not_null
    - name: tuu
      data_type: varchar(255)
      tests:
        - not_null
    - name: tduu
      data_type: varchar(255)
      tests:
        - not_null