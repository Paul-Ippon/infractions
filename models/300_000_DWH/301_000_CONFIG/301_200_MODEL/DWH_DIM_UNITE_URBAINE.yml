
version: 2

models:
  - name: 310_330_DIM_UNITE_URBAINE
    config:
      materialized: incremental
      database: "{{ target.database }}"
      schema: "DWH"
      alias: DIM_UNITE_URBAINE
      unique_key: uu2020
      incremental_strategy: merge
      merge_exclude_columns: ['inserted_at']
    columns:
    - name: uu2020
      data_type: varchar(5)
      tests:
        - not_null
    - name: tuu2017
      data_type: varchar(255)
      tests:
        - not_null
    - name: tduu2017
      data_type: varchar(255)
      tests:
        - not_null