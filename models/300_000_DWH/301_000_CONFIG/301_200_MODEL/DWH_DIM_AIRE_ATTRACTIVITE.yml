
version: 2

models:
  - name: 310_440_DIM_AIRE_ATTRACTIVITE
    config:
      materialized: incremental
      database: "{{ target.database }}"
      schema: "DWH"
      alias: DIM_AIRE_ATTRACTIVITE
      unique_key: aav2020
      incremental_strategy: merge
      merge_exclude_columns: ['inserted_at']
    columns:
    - name: aav2020
      data_type: varchar(3)
      tests:
        - not_null
        - unique
    - name: taav2017
      data_type: varchar(255)
      tests:
        - not_null
    - name: tdaav2017
      data_type: varchar(255)
      tests:
        - not_null