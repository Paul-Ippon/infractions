select 
    '{{ category }}' as category,
    '{{ code }}' as code,
    '{{ extra }}' as extra,
    '{{ invocation_id }}' as invocation_id,
    '{{ level }}' as level,
    '{{ log_version }}' as log_version,
    '{{ name }}' as name,
    '{{ ts }}' as ts