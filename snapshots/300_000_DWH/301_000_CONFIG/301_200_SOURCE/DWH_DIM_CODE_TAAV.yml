version: 2

sources:
  - name: DWH_DIM_CODE_TAAV
    database: "{{ target.database }}"
    schema: "{{ target.schema }}_DWH"
    tables:
      - name: DIM_CODE_TAAV
        columns:
          - name: VARIABLE
          - name: CODE
          - name: LIBELLE
