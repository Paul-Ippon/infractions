version: 2

sources:
  - name: DWH_DIM_ZON_COM_EPCI
    database: "{{ target.database }}"
    schema: "{{ target.schema }}_DWH"
    tables:
      - name: DIM_ZON_COM_EPCI
        columns:
          - name: CODE_EPCI
          - name: NATURE_EPCI