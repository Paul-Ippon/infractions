version: 2

sources:
  - name: DWH_DIM_CODE_TYPO_DEGRE_DENSITE
    database: "{{ target.database }}"
    schema: "{{ target.schema }}_DWH"
    tables:
      - name: DIM_CODE_TYPO_DEGRE_DENSITE
        columns:
          - name: VARIABLE
          - name: CODE
          - name: LIBELLE
