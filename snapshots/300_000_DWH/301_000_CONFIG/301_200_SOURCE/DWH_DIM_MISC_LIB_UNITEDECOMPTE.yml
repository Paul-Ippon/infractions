version: 2

sources:
  - name: DWH_DIM_MISC_LIB_UNITEDECOMPTE
    database: "{{ target.database }}"
    schema: "{{ target.schema }}_DWH"
    tables:
      - name: DIM_MISC_LIB_UNITEDECOMPTE
        columns:
          - name: CODE
          - name: LIBELLE
